[project]
name = "sumoffice"
authors = [{ name = "SumOffice" }]
description = "Open and edit Excel and Word attachments in SumOffice over WOPI"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
